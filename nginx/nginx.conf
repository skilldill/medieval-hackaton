server {  
  listen 80;
  server_name medieval-hackaton.ru;
  location / {
    # This would be the directory where your React app's static files are stored at
    proxy_pass http://localhost:8080;
    root /usr/share/nginx/html;
    try_files $uri /index.html;
  }
}
